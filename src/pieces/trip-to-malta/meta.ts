import type { PieceManifest } from '../types'

export const meta = {
  slug: 'trip-to-malta',
  title: 'Trip to Malta',
  description: 'Here’s a photo dump from Malta.',
  status: 'approved',
  mode: 'hosted',
  order: 1,
} satisfies PieceManifest
