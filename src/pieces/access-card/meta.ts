import type { PieceManifest } from '../types'

export const meta = {
  slug: 'example1',
  title: 'Access Card',
  description: 'RBAC device motion component.',
  status: 'approved',
  mode: 'hosted',
  order: 3,
} satisfies PieceManifest
